#include <stdio.h>
#include <stdlib.h>

int main()
{
//    printf("Operadores de asignacion\n\n");
//
//    int value;
//
//    value = 11; // = es el operador de asignacion
//    printf("El valor de nuestra variable es: %i\n", value);
//
//    value += 1; // += es lo mismo que si usaramos value = value + 1
//    printf("El valor de nuestra variable es: %i\n", value);
//
//    value -= 2; // -= es lo mismo que si usaramos value = value - 2
//    printf("El valor de nuestra variable es: %i\n", value);
//
//    value *= 5; // *= es lo mismo que si usaramos value = value * 5
//    printf("El valor de nuestra variable es: %i\n", value);
//
//    value /= 4; // /= es lo mismo que si usaramos value = value / 5
//    printf("El valor de nuestra variable es: %i\n", value);
//
//    value %= 6; // %= nos muestra el modulo de value / 6
//    printf("El valor de nuestra variable es: %i\n", value);

// Reto

    int x = 10;


    x += 2*x;

    printf("X: %i\n", x);

    return 0;
}
